/**
 * Kept in step with the version in package.json. The User-Agent embeds it, so
 * Supertoinette can tell which build is calling from its own logs.
 */
export const PKG_VERSION = "0.1.0";
export const REPO_URL = "https://github.com/smeet666/mcp-supertoinette";
